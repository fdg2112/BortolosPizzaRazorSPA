@page
@model IndexModel
@{
    ViewData["Title"] = "El lugar para los amantes de la Pizza";
    TimeSpan timeInBusiness = DateTime.Now - new DateTime(2018, 8, 14);
}

<div class="text-center">
    <h1 class="display-4">Bienvenidos a Bartolo's Pizza!</h1>
    <p class="lead">La mejor pizza de la ciudad durante los últimos @Convert.ToInt32(timeInBusiness.TotalDays) días!</p>
</div>
